# Uniguard Pro Bridge — environment overrides
# Copy to .env and adjust as needed.
# All values have sane defaults; only set what you need to change.

# Network binding
UGBRIDGE_HOST=0.0.0.0
UGBRIDGE_PORT=8080

# Stream lifetime: seconds of inactivity before auto-stop (default 300 = 5 min)
UGBRIDGE_STREAM_TIMEOUT_SECONDS=300

# Path to ffmpeg binary (default: 'ffmpeg' on PATH)
# UGBRIDGE_FFMPEG_PATH=/usr/bin/ffmpeg

# HLS output directory (must be writable)
# UGBRIDGE_HLS_DIR=/opt/uniguard-bridge/hls

# SQLite database path
# UGBRIDGE_DB_PATH=/opt/uniguard-bridge/uniguard.db

# LAN scan: seconds to wait per host TCP probe (lower = faster but more misses)
UGBRIDGE_SCAN_TIMEOUT_SECONDS=1.5

# HLS segment length in seconds (2 is good for low latency)
UGBRIDGE_HLS_SEGMENT_TIME=2

# Number of HLS segments kept in the playlist (6 × 2 s = 12 s buffer)
UGBRIDGE_HLS_LIST_SIZE=6
